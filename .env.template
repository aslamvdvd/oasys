# OASYS Environment Configuration Template
# Copy this file to .env and fill in the appropriate values

# Debug configuration (True for development, False for production)
DEBUG=False

# Security key (generate a new one for production!)
# You can generate a new key using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your_secret_key_here

# Database connection string
# Format: postgres://username:password@hostname:port/database_name
DATABASE_URL=postgres://username:password@localhost:5432/database_name

# File Storage Configuration
# Uncomment and set these variables to override the default paths

# Logs directory path
# LOGS_PATH=/path/to/logs

# Media files storage path (user uploads)
# MEDIA_ROOT_PATH=/path/to/media

# Static files collection path
# STATIC_ROOT_PATH=/path/to/staticfiles

# Templates storage path
# TEMPLATE_UPLOAD_PATH=/path/to/templates_store

# Static files URL prefix (must end with /)
# STATIC_URL=static/

# Media files URL prefix (must end with /)
# MEDIA_URL=media/

# Additional settings
# Add any other environment-specific settings below 